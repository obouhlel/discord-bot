{
  "name": "discord-bot",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "docker compose -f docker-compose.yml up -d; sleep 3; bun --watch ./src/index.ts",
    "server": "bun ./src/index.ts",
    "migrate:dev": "bunx prisma migrate dev --name init",
    "migrate:prod": "bunx prisma migrate deploy",
    "generate": "bunx prisma generate"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "eslint": "^9.26.0",
    "prisma": "^6.7.0"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@huggingface/inference": "^3.12.1",
    "@prisma/client": "^6.7.0",
    "axios": "^1.9.0",
    "discord.js": "^14.19.3",
    "dotenv": "^16.5.0",
    "fastify": "^5.3.2",
    "fastify-plugin": "^5.0.1"
  }
}
