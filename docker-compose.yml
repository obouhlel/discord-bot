services:
  bot:
    container_name: bot_discord
    build: .
    env_file:
      - .env
    expose:
      - "3000"
    restart: "on-failure"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
